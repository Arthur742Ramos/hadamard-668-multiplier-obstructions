# Release assets

Large generated CNFs and DRAT traces are not stored in Git. Release `v1.0.0`
provides:

```text
proof-artifacts-v1.0.0.tar.zst
proof-artifacts-v1.0.0.tar.zst.sha256
```

The archive extracts into `lp333/proof_phase2/`.
